{% extends "::base.html.twig" %}

{% block body %}
    {#{% if app.user is not empty %}#}
        {#{{ dump(app.user) }}#}
    {#{% endif %}#}
    <div id="app"></div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script>
        const data = {
            user: {% if app.user is not empty %}{{ app.user|serialize('json')|raw }}{% else %}null{% endif %},
            notes: [],
            note: {}
        };
    </script>

    <script>
        const datas = {
            user: {% if app.user is not empty %}{{ app.user|serialize('json')|raw }}{% else %}"toto"{% endif %},
            staticFolder :"{{ asset("") }}"
        };
    </script>
    <script>require('Resources/build/vue/index.js');</script>
{% endblock %}